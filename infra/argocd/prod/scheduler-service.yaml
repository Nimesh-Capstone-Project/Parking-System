apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: smart-parking-prod-scheduler-service
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
    ci.smartparking.io/last-release-sha: "05ebf32b879331983629febe5d1cfb80b32c0736"
    ci.smartparking.io/last-release-tag: "v1.0.7"
    ci.smartparking.io/last-released-at: "2026-04-27T13:00:41Z"
    ci.smartparking.io/last-released-by: "nimeshsv814"
spec:
  project: default
  source:
    repoURL: https://github.com/Nimesh-Capstone-Project/Parking-System.git
    targetRevision: master
    path: infra/helm
    helm:
      valueFiles:
        - values/scheduler-service.yaml
      parameters:
        - name: schedulerService.deployment.containers[0].image.repository
          value: ghcr.io/nimesh-capstone-project/scheduler-service
        - name: schedulerService.deployment.containers[0].image.tag
          value: v1.0.7
      values: |
        namespace: prod
        authService:
          enabled: false
        bookingService:
          enabled: false
        frontend:
          enabled: false
        gatewayApi:
          gateway:
            enabled: false
          httpRoute:
            enabled: false
        mongo:
          enabled: false
        notificationService:
          enabled: false
        parkingService:
          enabled: false
        paymentService:
          enabled: false
  destination:
    server: https://kubernetes.default.svc
    namespace: prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
