apiVersion: v1
kind: Service
metadata:
  name: mongo-svc
  namespace: sps-ns
spec:
  selector:
    app: sps-mongo
  ports:
    - port: 27017
      targetPort: 27017
  type: ClusterIP
  clusterIP: None